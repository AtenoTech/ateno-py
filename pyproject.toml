[project]
name = "ateno"
version = "0.1.3" # Bump the version so the Robot sees a change
description = "Python SDK for Ateno"
authors = [{name = "Ateno Inc."}]
dependencies = ["requests"]

# ADD THIS SECTION:
[project.scripts]
ateno = "ateno.main:main" 

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"